# Computer-Networks-Project
A multi-threaded HTTP web server implemented in Python, capable of serving static files, handling concurrent client connections, and providing secure HTTP request processing with proper error handling.
